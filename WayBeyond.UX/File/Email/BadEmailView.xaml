<UserControl x:Class="WayBeyond.UX.File.Email.BadEmailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WayBeyond.UX.File.Email"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <b:CallMethodAction TargetObject="{Binding}" MethodName="OnViewLoaded"/>
        </b:EventTrigger>
    </b:Interaction.Triggers>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button FontFamily="{StaticResource FontAwesome}" FontSize="14" Margin="3" Command="{Binding AddBadEmailCommand}"
                Background="Transparent" BorderThickness="0">
                <StackPanel Orientation="Horizontal" Margin="3">
                    <TextBlock Text="&#xf055;"/>
                    <TextBlock Text="Ba;d Email" Margin="3,0,0,0"/>
                </StackPanel>
            </Button>
        </StackPanel>
        <DataGrid Grid.Row="1" ItemsSource="{Binding BadEmails}"
                  AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="2*"/>
                <DataGridTextColumn Header="Domain" Binding="{Binding Domain}" Width="2*"/>
                <DataGridTextColumn Header="Full Email" Binding="{Binding EmailAddress}" Width="3*"/>
                <DataGridTextColumn Header="Type" Binding="{Binding BadEmailType}" Width="*"/>
                <DataGridTemplateColumn Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="trash" FontFamily="{StaticResource FontAwesome}" Margin="3"
                                Command="{Binding DataContext.DeleteEmailCommand, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                                CommandParameter="{Binding}" Background="Transparent" BorderThickness="0"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>

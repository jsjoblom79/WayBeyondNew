<UserControl x:Class="WayBeyond.UX.File.Location.AddEditFileLocationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WayBeyond.UX.File.Location"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            
        </Grid>
        <Grid Grid.Row="1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Center">
                <Button Content="Add" Command="{Binding AddEditFileLocationCommand, Converter={StaticResource }}" Width="75" Margin="3"/>
                <Button Content="Edit" Command="{Binding AddEditFileLocationCommand}" Width="75" Margin="3"/>
                <Button Content="Cancel" Command="{Binding CancelCommand}" Width="75" Margin="3"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
